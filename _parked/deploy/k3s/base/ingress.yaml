apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-auth
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-strip-apiv1@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /apiv1
        pathType: Prefix
        backend:
          service:
            name: auth-service
            port:
              number: 8080

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-uploads
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-uploads-cache@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /uploads
        pathType: Prefix
        backend:
          service:
            name: auth-service
            port:
              number: 8080

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-lms
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-rewrite-lms@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /lmsapiv1
        pathType: Prefix
        backend:
          service:
            name: lms-service
            port:
              number: 8081

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-files
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-rewrite-files@kubernetescrd,
      default-files-cache@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /files
        pathType: Prefix
        backend:
          service:
            name: lms-service
            port:
              number: 8081

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-lab
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-rewrite-lab@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /labapiv1
        pathType: Prefix
        backend:
          service:
            name: lab-service
            port:
              number: 8082

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-chat
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-rewrite-chat@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /chatapiv1
        pathType: Prefix
        backend:
          service:
            name: chat-service
            port:
              number: 8083

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-personalize
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /personalize
        pathType: Prefix
        backend:
          service:
            name: personalize-service
            port:
              number: 8082
      - path: /personalize-dashboard
        pathType: Prefix
        backend:
          service:
            name: personalize-service
            port:
              number: 8082

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-frontend
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3001

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bdc-ingress-mcp
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: >-
      default-compress@kubernetescrd,
      default-mcp-ratelimit@kubernetescrd
spec:
  rules:
  - host: bdc.hpcc.vn
    http:
      paths:
      - path: /mcp
        pathType: Prefix
        backend:
          service:
            name: ai-service
            port:
              number: 8000

---
# ── Traefik Middleware: Coarse edge protection for public MCP ────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mcp-ratelimit
  namespace: default
spec:
  rateLimit:
    average: 30
    burst: 60
    period: 1m

---
# ── Traefik Middleware: Global Compression ────────────────────────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: compress
  namespace: default
spec:
  compress: {}

---
# ── Traefik Middleware: Strip /apiv1 prefix for Auth Service ─────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-apiv1
  namespace: default
spec:
  stripPrefix:
    prefixes:
      - /apiv1

---
# ── Traefik Middleware: Rewrite /lmsapiv1 → /api/v1 ─────────────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rewrite-lms
  namespace: default
spec:
  replacePathRegex:
    regex: "^/lmsapiv1/(.*)"
    replacement: "/api/v1/$1"

---
# ── Traefik Middleware: Rewrite /files → /api/v1/files/serve ─────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rewrite-files
  namespace: default
spec:
  replacePathRegex:
    regex: "^/files/(.*)"
    replacement: "/api/v1/files/serve/$1"

---
# ── Traefik Middleware: Rewrite /labapiv1 → /api/v1 ─────────────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rewrite-lab
  namespace: default
spec:
  replacePathRegex:
    regex: "^/labapiv1/(.*)"
    replacement: "/api/v1/$1"

---
# ── Traefik Middleware: Rewrite /chatapiv1 → /api/v1 ────────────────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rewrite-chat
  namespace: default
spec:
  replacePathRegex:
    regex: "^/chatapiv1/(.*)"
    replacement: "/api/v1/$1"

---
# ── Traefik Middleware: Cache headers for /uploads (auth service files) ───────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: uploads-cache
  namespace: default
spec:
  headers:
    customResponseHeaders:
      Cache-Control: "public, max-age=31536000, immutable"

---
# ── Traefik Middleware: Cache headers for /files (LMS served files) ───────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: files-cache
  namespace: default
spec:
  headers:
    customResponseHeaders:
      Cache-Control: "public, max-age=31536000, immutable"
